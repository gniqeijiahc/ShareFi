<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <solid android:color="@color/light_blue" /> <!-- Blue color -->
    <padding
        android:left="9dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp"/>
    <!-- Border color and width -->
    <stroke
        android:width="2dp"
        android:color="@color/white" />

    <!-- Corner radius -->
    <corners android:radius="15dp" />
</shape>
